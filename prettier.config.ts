import { type Config } from 'prettier'

const config: Config = {
  trailingComma: 'all',
  semi: false,
  arrowParens: 'avoid',
  bracketSpacing: true,
  bracketSameLine: true,
  objectWrap: 'collapse',
}

export default config
